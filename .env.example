# Variáveis da solução. Nunca preencha ou versione uma chave real.
#
# Agente de usabilidade (src/usability-agent): lê estas chaves do ambiente do processo;
# não carrega arquivos .env automaticamente.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
#
# Backend do copiloto: copie src/backend/.env.example para src/backend/.env.
# Frontend: copie src/frontend/.env.example para src/frontend/.env.local (opcional; vazio usa os mocks).
